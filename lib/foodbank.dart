import 'package:flutter/material.dart';

class FoodBank extends StatelessWidget {
  const FoodBank({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: Text("foodbank"));
  }
}
