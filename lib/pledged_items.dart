import 'package:flutter/material.dart';

class PledgedItems extends StatelessWidget {
  const PledgedItems({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
